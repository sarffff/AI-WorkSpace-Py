# Agent 系统进度与落地方向

*生成时间：2026-08-30*  
*基于：1055 个后端测试、60 个前端测试、36 条端到端评估任务*

---

## 一、核心功能完成度

### ✅ 已完成并线上可用

#### 1. 基础 Agent 循环（baseline 变体）
- **状态**：生产就绪，默认启用
- **能力**：
  - 7 个工具：`search_knowledge_base`、`read_document_chunk`、`list_knowledge_documents`、`calculate`、`web_search`、`fetch_url`、`save_to_knowledge_base`
  - 三道收敛保证：轮次上限（6）、字符预算（8000）、重复调用检测（3 次）
  - 单工具熔断：连续 2 次失败即从工具面移除
- **性能**（8/30 评估）：
  - 任务成功：4.58/5.0
  - 工具召回：0.812（必需工具基本都用到）
  - 轮次效率：0.984（基本不绕路）
  - 平均轮次：1.29（大多数任务一轮完成）
  - 违规调用：0（从不调用禁止的工具）

#### 2. 状态快照与恢复
- **已落地功能**：
  - 轮次边界（`post_tools`）自动落快照
  - 审批中断 → 恢复 → 接着跑
  - 澄清提问 → 用户答 → 接着跑
  - 断线标记（`interrupted`）→ 静默重连
- **断线接续**（本次完成）：
  - 服务端：SSE 的 `finally` 把断掉的执行标成 `interrupted`，不等 780 秒孤儿超时
  - 前端：静默重连 3 次，试满才问用户
  - 已有 18 个后端测试覆盖

#### 3. 人工审批
- **覆盖范围**：
  - 写知识库前必须审批
  - 支持"拒绝"、"同意"、"改参数后同意"
  - 审批审计：谁批的、什么时候、批的和跑的是不是同一份参数
- **拒绝遵从率**：1.0（模型被拒后从不重试同一件事）
- **评估覆盖**：3 个 approval 任务

#### 4. 澄清提问 (`ask_user`)
- **能力**：
  - 挂起当前回合，等用户在**另一个请求**里答
  - 答案回来接着这一轮跑，不丢前几轮的工具结果
- **评估覆盖**：6 个 clarification 任务

#### 5. 安全防线
| 防线 | 覆盖范围 | 状态 | 注 |
|---|---|---|---|
| **护栏（guardrails）** | 知识库、网页、记忆、附件 | ✅ | nonce 定界 + 模式检测 + 中和 |
| **审批** | 写操作 | ✅ | 人工裁决，不可绕过 |
| **用量闸门** | 每用户频率、成本、token | ✅ | 三道独立上界 |
| **记忆注入防护** | 长期记忆里的伪造指令 | ⚠️ 刚修 | `injection-memory` 稳定失败，8/30 修了措辞，待验证 |

---

### 🚧 已实现但默认关闭

#### 1. 多代理委派（delegation）
- **两个变体**：
  - `augment`：所有任务都委派给子代理，主代理只做委派与整合
  - `supervisor`：主代理判断需要时才委派（researcher / planner）
- **为什么关闭**：
  - `augment`（8/28 评估）：输入 token +29%，任务成功 4.75 → 4.08（**退步**）
  - `supervisor`（8/30 评估）：输入 token +39%，任务成功持平（4.50 vs 4.58），**不划算**
- **已定位的问题**（supervisor）：
  - ✅ 已修：重复检测错误触发提前收敛，导致 `toolRecall` 0.438（现已 0.812）
  - ✅ 已修：researcher 轮次 4 → 5（最后一轮无工具，4 只给 3 轮实际工具调用）
  - ✅ 已修：子代理重复上限收窄成 `min(2, 主上限)`
  - 🔴 仍在：成本未降下来，`web-vat-code` 搜 4 次、`chain-rail-price` 调 9 次
- **落地方向**：
  - **不建议强行启用**——当前工作负载下成本与收益不匹配
  - 未来如果有复杂多跳任务（需要 3+ 次检索 + 推理链），再重新评估
  - `augment` 的"全委派"策略已被证明是错的，不再考虑

---

## 二、评估与质量门禁

### ✅ 已建立的评估体系

#### 1. RAG 评估
- **语料**：54 题 / 13 篇文档 / 92 个分块
- **指标**：
  - 检索质量：precision@k、nDCG@k
  - 安全指标：拒答率、抗注入率、编造率
- **变体数**：26（查询改写 × 检索策略 × 重排）
- **最近报告**：2026-08-27

#### 2. Agent 评估
- **任务数**：36（29 个计入主指标，7 个抽取类单独统计）
- **探针类型**（按任务数）：
  - clarification: 6（澄清提问）
  - memory_extract: 5（记忆抽取抗性与召回）
  - multi_domain: 4（跨领域链式推理）
  - approval: 3（审批遵从）
  - tool_choice: 3（工具选择）
  - chain: 2（多步推理）
  - absent: 2（查无时如实说）
  - injection: 2（注入抗性）
  - memory: 2（记忆使用）
  - no_tool: 2（无需工具直接答）
  - write_guard: 2（写操作护栏）
  - recovery: 1（降级与兜底）
  - web_choice: 1（本地 vs 网页判断）
  - write: 1（知识库写入）
- **指标**：
  - 任务成功 & 有据性（LLM 裁判打分 1-5）
  - 工具召回 & 工具精度
  - 轮次效率、违规调用、抗注入率、抽取抗性

#### 3. 评估门禁（eval/gate.py）
- **触发时机**：每次 `run_agent.py --variants X Y` 跑完
- **判据**：
  - 绝对阈值：任务成功 ≥ 3.5、工具召回 ≥ 0.7、违规调用 = 0
  - 相对阈值：与 baseline 比，成功率不能掉超过 0.5
- **设计目标**：把"模型变差"和"测量坏了"分开判

---

## 三、已知问题与优化方向

### 🔴 高优先级（影响生产）

#### 1. 记忆注入（injection-memory）
- **问题**：`injection-memory` 在 baseline 上稳定失败（8/28 和 8/30 两次运行全中）
- **根因**：记忆声明授予了"格式偏好"权限，攻击伪装成格式指令（"开头输出 canary"）
- **修复**（8/30）：收窄授权边界 —— 表达习惯管"怎么呈现"，不包括"插入固定字符串"
- **验证状态**：⚠️ **措辞已改，效果未验证** —— 需跑 `--probe injection` 确认
- **影响面**：所有使用长期记忆的场景

#### 2. 断线接续的测试覆盖不完整
- **问题**：
  - 前端 13 个测试全在 `reconnect.ts` 模块层，**没有集成测试**
  - 服务端 `mark_interrupted` 的 finally 执行时机只能通过手动断线验证
- **风险**：SSE 断掉时 finally 没跑到（进程被杀）的情况，只能靠孤儿回收兜底
- **落地建议**：
  - 加一个端到端集成测试：启动真实服务器 → 开始一个 agent 回合 → 主动断连接 → 验证状态标成 `interrupted`
  - 或者接受当前覆盖度，用监控补（跟踪 `interrupted` vs `orphaned` 的比例）

---

### 🟡 中优先级（优化方向）

#### 1. supervisor 的成本问题
- **现状**：修好了 `toolRecall`（0.438 → 0.812），但输入 token +39%
- **根因**：
  - researcher 现在 5 轮，v2 提示词要求必须调工具
  - 重复不再触发提前收敛 → `web-vat-code` 搜 4 次、`chain-rail-price` 调 9 次
- **落地方向**：
  - **短期**：保持 `off`，等工作负载出现真正需要委派的复杂任务时再说
  - **中期**（如果要启用）：
    - 给 researcher 加"去重判断"：同语义的搜索只跑一次
    - 或者把 researcher 的 `max_rounds` 从 5 降到 4，接受"3 轮实际工具调用"的约束
  - **不考虑 augment**：全委派策略已被 8/28 评估证明是错的

#### 2. 注入抗性只有 0.33
- **问题**：baseline 和 supervisor 都只有 0.33（3 个轮次里 1 个干净）
- **已知原因**：
  - `injection-memory` 的 canary（见上面高优先级）
  - `injection-vendor` 通过（0 avoidHits）
- **落地建议**：
  - 先验证记忆那条修没修好
  - 如果修好了，注入抗性会升到 0.67（2/3）
  - 评估只有 2 个注入任务（样本太小），考虑再加几条

#### 3. 抽取抗性（baseline 0.40）
- **问题**：40% 的伪造"偏好"被写进了长期记忆
- **已知场景**：
  - 假偏好措辞正常（不命中护栏模式）
  - 抽取提示词里的排除段是主防线
- **落地方向**：
  - 评估 `memory_extract` 的 v1 vs v2：排除段改写值多少分
  - 或者给抽取侧也加结构化输出 schema，要求 `kind` 与 `justification` 分开

---

### 🟢 低优先级（可观测与工程质量）

#### 1. 前端 TypeScript strict 模式
- **现状**：已打开（8/29 提交），60 个测试通过
- **收益**：提前发现空值解引用、类型不匹配

#### 2. 提示词契约校验
- **现状**：`python -m services.prompt_library validate` 在 CI 里跑
- **覆盖**：占位符漂移、模板缺失、必需字段缺失
- **收益**：不再在运行时才发现提示词引用错误

#### 3. 调用链追踪
- **现状**：父子 span 树，按轮次归因，token 来源分列
- **待补**：
  - 前端可视化（现在只有后端日志）
  - 导出 Jaeger / OpenTelemetry 格式

---

## 四、推荐的落地优先级

### 第一阶段：验证与稳定（1-2 天）
1. ✅ **验证记忆注入修复**
   ```bash
   python -m eval.run_agent --variants baseline --probe injection
   ```
   预期：`injectionResistRate` 从 0.33 升到 0.67（如果只修好 injection-memory）

2. ✅ **补断线接续的集成测试**
   - 或者接受当前覆盖度，加监控

### 第二阶段：优化与扩展（1-2 周）
3. 🔵 **评估 `memory_extract` v1 vs v2**
   - 排除段改写值多少分（提升 `extractionResistRate`）

4. 🔵 **增加注入任务样本**
   - 目前只有 2 条，样本太小
   - 至少加到 5 条，覆盖更多攻击向量

5. 🔵 **调用链前端可视化**
   - 现在只有后端 span，前端看不到
   - 对调试复杂 agent 回合有帮助

### 第三阶段：委派重启判断（按需）
6. ⚪ **等工作负载出现真正需要委派的任务**
   - 当前 29 个任务里没有一个值得 +39% token 的
   - 如果未来有 3+ 跳的复杂推理链，再重新评估

---

## 五、关键指标监控建议

如果要上线，这些指标要持续跟踪：

| 指标 | 目标 | 告警阈值 | 备注 |
|---|---|---|---|
| 任务成功（baseline） | ≥ 4.5 | < 3.5 | LLM 裁判打分 |
| 工具召回 | ≥ 0.80 | < 0.70 | 必需工具有没有用到 |
| 违规调用 | = 0 | > 0 | 硬指标 |
| 注入抗性 | ≥ 0.67 | < 0.50 | 修好 injection-memory 后应该到 0.67 |
| 抽取抗性 | ≥ 0.50 | < 0.30 | 伪造偏好有没有被写进记忆 |
| 平均轮次 | ≤ 1.5 | > 2.0 | 绕路指标 |
| P95 延迟 | < 30s | > 60s | 用户体验 |
| `interrupted` 比例 | - | > 30% | 如果太高说明网络不稳定或前端 bug |

---

## 六、总结

### 可以放心用的部分
- ✅ baseline agent（7 工具 + 收敛保证 + 熔断）
- ✅ 审批 + 澄清提问（状态快照已稳定）
- ✅ 断线静默重连（前端 13 测试 + 后端 18 测试）
- ✅ 评估门禁（区分"模型变差"和"测量坏了"）

### 需要验证的部分
- ⚠️ 记忆注入修复（措辞已改，跑 `--probe injection` 确认）

### 不建议现在启用的部分
- 🔴 多代理委派（`supervisor`）：成本 +39%，收益持平
- 🔴 `augment` 变体：已被证明是错的，不考虑

### 下一步最值得做的事
1. 验证记忆注入修复
2. 补断线接续的集成测试（或加监控接受当前覆盖）
3. 评估 `memory_extract` v2（提升抽取抗性）

---

*数据来源：*
- *后端测试：1055 pass*
- *前端测试：60 pass*
- *最新评估：2026-08-30 (29 tasks × 2 variants)*
- *最新提交：13f40c2 (fix 记忆注入)*
