# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
dist
dist-electron
dist-ssr
release
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage
*.lcov
.nyc_output

# Turbo
.turbo

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Electron
#
# 这几条必须锚在根上（前导 /）。gitignore 里不带斜杠的 `app/` 会在**任意层级**
# 匹配，于是它连 `src/app/` 一起吞掉——那里面是 App.tsx、Layout.tsx、
# providers/store.ts 和 main.tsx，而 main.tsx 正是 index.html 引用的入口。
# 后果是版本库里从来没有这四个文件，克隆下来直接构建不了，而本地开发一切正常，
# 所以没人发现。electron-builder 的实际输出目录是 release/（见 package.json 的
# directories.output），`app/` 这条本来就没用上。
/out/
/release/
*.exe
*.dmg
*.app
*.AppImage

# OS
Thumbs.db
.DS_Store
desktop.ini

# Temporary files
*.tmp
*.temp
*.cache
.cache/

# Package manager lock files (optional - uncomment if needed)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Debug
*.pid
*.seed
*.pid.lock

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/*

# IDE - WebStorm
.idea/
*.iml
*.iws

# IDE - Sublime Text
*.sublime-workspace

# Misc
*.swp
*.swo
*~
.eslintcache
