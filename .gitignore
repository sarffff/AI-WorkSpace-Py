# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
# 这一段是 Python 打包产物，全部锚在仓库根上。
#
# 不锚的话 `lib/` 会在任意层级匹配，把前端的 `src/shared/lib/` 和
# `src/entities/auth/lib/` 一起吞掉——那里面是 ThemeContext、ProtectedRoute、
# AuthProvider、diffLines、format，全都是 App 直接 import 的源码。
# 症状是"本地跑得好，克隆下来构建不了"，而且 git status 干净，没有任何提示。
# 同类风险还有 build/ dist/ var/ parts/：前端目录树里出现同名子目录就会中招。
/build/
/develop-eggs/
/dist/
/downloads/
/eggs/
/.eggs/
/lib/
/lib64/
/parts/
/sdist/
/var/
/wheels/
*.egg-info/
# 后端自己那一层的打包产物。锚到 back-end/ 而不是写成裸模式，理由同上：
# 裸 build/ 会连前端目录树里的同名子目录一起吞。
back-end/build/
back-end/dist/
back-end/lib/
back-end/sdist/
back-end/var/
back-end/wheels/
.installed.cfg
*.egg
MANIFEST
venv/
ENV/
env/

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# 环境变量
.env
.env.local
.env.*.local

# 数据库
*.db
*.sqlite
*.sqlite3

# 日志
*.log
logs/

# 操作系统
Thumbs.db
desktop.ini

# 临时文件
*.tmp
*.temp
*.cache
.cache/

# 编译输出
*.pyc
*.pyo
*.pyd
.Python
*.o
*.out

# 测试
.pytest_cache/
.coverage
htmlcov/
.tox/

# 评估报告改为进版本库:变体对比的价值在于"改了配置前后有数据可回看",
# 报告留在本地等于每次都从零开始。真正每次运行都会变、不值得提交的是
# 报告生成过程中的临时文件,而不是结论本身。

# 从线上反馈导出的回归用例：内容是真实对话里的提问与期望答案，
# 属于用户数据，不进版本库。rag_golden.jsonl 是自造语料，可以提交。
back-end/eval/datasets/feedback_regression.jsonl

# 真实价目表（示例见 model_prices.example.json）
back-end/model_prices.json

# 运行时上传的文件。文件名是随机 hash，内容是谁在本地点过"上传"的产物，
# 对别人一律没有意义——之前混进版本库的是两张 1.33 MB 的同图 PNG、一份
# security-policy.md 的手工上传副本和一个两行的 probe.md，都无人引用。
# eval 的附件是真夹具（agent_stubs.py 直接按路径读它），单独放行。
back-end/uploads/
!back-end/uploads/eval/
!back-end/uploads/eval/**
